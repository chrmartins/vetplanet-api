plugins {
	id 'java'
	id 'org.springframework.boot' version '4.1.0'
	id 'io.spring.dependency-management' version '1.1.7'
}

group = 'com.rafaelasoares'
version = '0.0.1-SNAPSHOT'
description = 'API do sistema Dra. Rafaela Soares - atendimento veterinario domiciliar'

java {
	toolchain {
		languageVersion = JavaLanguageVersion.of(21)
	}
}

repositories {
	mavenCentral()
}

dependencies {
	implementation 'org.springframework.boot:spring-boot-starter-actuator'
	implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
	implementation 'org.springframework.boot:spring-boot-starter-flyway'
	implementation 'org.springframework.boot:spring-boot-starter-security'
	implementation 'org.springframework.boot:spring-boot-starter-validation'
	implementation 'org.springframework.boot:spring-boot-starter-webmvc'
	implementation 'org.flywaydb:flyway-database-postgresql'
	// OpenAPI/Swagger. A 3.1.0 é construída sobre o spring-boot-starter-parent
	// 4.1.0 — mesma linha do Boot usado aqui. A série 2.x é para Boot 3.x e
	// NÃO serve. Atenção: o índice de busca do Maven Central ainda lista só
	// 2.x; quem manda é o maven-metadata.xml do repositório.
	//
	// A documentação fica DESLIGADA por padrão e só liga no perfil `dev`
	// (ver application.yml / application-dev.yml). É proposital: ela mapeia
	// endpoints que manipulam CPF e prontuário.
	implementation 'org.springdoc:springdoc-openapi-starter-webmvc-ui:3.1.0'
	compileOnly 'org.projectlombok:lombok'
	// Sobe o docker-compose.yml junto com a aplicação e espera o banco ficar
	// saudável antes de conectar. É o que faz `./gradlew bootRun` bastar.
	// `developmentOnly`: não entra no jar de produção, onde o banco é externo.
	developmentOnly 'org.springframework.boot:spring-boot-docker-compose'
	runtimeOnly 'org.postgresql:postgresql'
	annotationProcessor 'org.projectlombok:lombok'
	testImplementation 'org.springframework.boot:spring-boot-starter-actuator-test'
	testImplementation 'org.springframework.boot:spring-boot-starter-data-jpa-test'
	testImplementation 'org.springframework.boot:spring-boot-starter-flyway-test'
	testImplementation 'org.springframework.boot:spring-boot-starter-security-test'
	testImplementation 'org.springframework.boot:spring-boot-starter-validation-test'
	testImplementation 'org.springframework.boot:spring-boot-starter-webmvc-test'
	testImplementation 'org.springframework.boot:spring-boot-testcontainers'
	testImplementation 'org.testcontainers:testcontainers-junit-jupiter'
	testImplementation 'org.testcontainers:testcontainers-postgresql'
	testCompileOnly 'org.projectlombok:lombok'
	testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
	testAnnotationProcessor 'org.projectlombok:lombok'
}

// Avisa sobre API depreciada e cast inseguro. Vale especialmente porque o
// Spring Boot 4 é recente: assinatura que mudou desde o 3.x aparece aqui em
// vez de passar silenciosa.
tasks.withType(JavaCompile).configureEach {
	options.compilerArgs << '-Xlint:deprecation' << '-Xlint:unchecked'
}

tasks.named('test') {
	useJUnitPlatform()
}

// `./gradlew bootRun` é sempre desenvolvimento local, então ativa o perfil
// `dev` (que semeia o administrador inicial). Um `java -jar` — como sobe em
// produção — não passa por aqui e portanto não semeia nada.
tasks.named('bootRun') {
	systemProperty 'spring.profiles.active', 'dev'
}
